{{ form_start(form) }}
{{ form_widget(form) }}

<div class="w-100 d-flex justify-content-center align-items-center gap-3">
	<a href="{{ path('app_original_car_index') }}" class="btn  btn-outline-secondary">Zurück</a>
	<button class="btn btn-primary">{{ button_label|default('Speichern') }}</button>
	{% if "edit" in app.current_route %}
		{{ include('original_car/_delete_form.html.twig') }}
	{% endif %}
</div>
{{ form_end(form) }}
